const FlexCenter = ({ children }) => {
  return <div className="min-h-screen w-full flex items-center justify-center bg-slate-50">{children}</div>;
};
FlexCenter.propTypes;

export default FlexCenter;
